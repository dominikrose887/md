# md

Repository for **MD Studio** — a GitHub-style Markdown desktop editor (Electron).

| | |
| --- | --- |
| **Application** | [`FigmaUI/`](./FigmaUI/) |
| **Changelog** | [`CHANGELOG.md`](./CHANGELOG.md) |
| **How to version & release** | [`FigmaUI/RELEASING.md`](./FigmaUI/RELEASING.md) |

Quick start (from `FigmaUI/`): `npm install` → `npm run dev` (web + Electron) or `npm run build:installer` for a Windows installer.